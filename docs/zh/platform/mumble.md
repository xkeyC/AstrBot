# 接入 Mumble

Mumble 适配器以机器人身份连接 Mumble 服务器（1.5 及以上），在频道和私聊里收发文字；开启实时语音后，还能在频道里用语音对话（全双工），语音由 Codex 实时语音驱动。

## 创建 AstrBot Mumble 平台适配器

进入 `机器人` 页面，点击 `+ 创建机器人`，选择 `Mumble`，打开 `启用` 后填写：

- `Mumble 服务器地址`、`Mumble 端口`（默认 `64738`）
- `机器人用户名`：机器人在 Mumble 里显示的名字
- `服务器密码`：服务器设了密码，或机器人已注册账号时填写
- `初始频道`：连接后进入的频道名，或从根频道起的路径（如 `游戏/闲聊`）；留空则留在默认频道
- `频道文字唤醒前缀`：默认 `!`，见下文

首次连接时会在 `data/mumble/<平台 ID>/` 下生成客户端证书，服务器据此识别机器人。管理员可以在 Mumble 客户端里注册机器人账号、授予权限；也可以在 `客户端证书` 里指定自己的证书。

## 会话与唤醒

- **整个服务器是一个群聊会话**（会话 ID `server`），机器人在哪个频道都不影响上下文。管理员可以随时把机器人拖到别的频道，回复会发到消息所在的频道。
- **私聊**按用户各自一个会话。用户 ID 取客户端证书哈希（官方客户端都有证书），配置管理员时填这个哈希；没有证书也没注册的用户只在本次连接内有效。
- Mumble 没有 @ 功能。**频道文字以唤醒前缀开头**（如 `!今天天气怎么样`）才会唤醒机器人，等同于 @ 机器人；私聊总是唤醒。全局唤醒前缀（如 `/` 指令）照常可用。

## 实时语音

需要 `Codex Agent 执行器` 登录**包含语音功能的 ChatGPT 订阅账号**。语音通过 WebRTC 连接 Codex 实时语音，不需要 API Key。

- 有人在机器人所在频道说话时自动接入。频道里**只有叫到机器人的名字才会回应**：这由提示词约束，偶尔可能插话。**对机器人私语**（whisper）总是回应，回复也以私语发回。
- 语音里交办的事情**交给配套会话的 Agent 处理**：频道语音对应服务器群聊，私语对应该用户的私聊。它作为这个会话的一轮对话来执行，和文字聊天共用同一个上下文、人格、工具、记忆和审批规则；文字消息和语音请求互相排队，会话正忙时机器人会先口头说一声，处理完再说结果。回答只念出来，不会在聊天里发文字。身份：频道语音分不清是谁在说话，一律按固定的“语音用户”执行，权限锁定为普通成员；私语按说话人本人的身份和权限执行。（如果开启了“会话隔离”（`unique_session`），群聊会按用户拆成多个会话，频道语音对应的仍是整个服务器那一个群会话。）
- **待机**：连续 `语音待机时间`（默认 300 秒）没有识别到语音，就断开实时语音；检测到新的人声时自动恢复，恢复时会补上刚才那句话。
- **闭麦 / 开麦**：在频道里发送 `唤醒前缀 + 闭麦`（或 `mute`，也可以用全局唤醒前缀），私聊里直接发 `闭麦`，机器人就不听也不说，并显示为静音；发 `开麦`（`unmute`）恢复。闭麦超过 1 分钟会断开语音会话。
- 可配置的语音选项：`语音唤醒名` 和 `语音唤醒别名`、`音色`（juniper、maple、spruce、ember、vale、breeze、arbor、sol、cove，默认 cove）、`实时语音模型`、`语音附加提示词`。配套会话当前人格里填了“语音人格”时，语音模型使用语音人格，不使用语音附加提示词。

## 本地语音后端（MiniCPM-o）

把 `语音后端` 设为 **本地 MiniCPM-o**，全双工对话就改由自己部署的 MiniCPM-o 4.5 完成，不需要 ChatGPT 订阅；查询、执行等任务仍交给上面所说的配套会话的 Agent（Codex）。

- **服务端**：llama.cpp-omni 的 `astrbot-omni` 分支（在上游基础上增加了音色克隆、强制播报和工具路由），模型用 `MiniCPM-o-4_5-Q4_K_M.gguf` 加 audio / tts / token2wav 三组 GGUF。推荐的启动参数：`-ngl 99 -c 8192 -fa on -ctk q8_0 -ctv q8_0 --temp 0.7 --top-k 100 --top-p 0.8 --repeat-penalty 1.05`，并设置环境变量 `OMNI_TTS_N_CTX=4096`、`OMNI_ROUTER_CTX=2048`；使用音色克隆时还要设置 `OMNI_VOICE_BUNDLE_CMD`，指向 `tools/omni/voice/make_voice_bundle.py`（需要 `campplus.onnx`、`speech_tokenizer_v2_25hz.onnx`）。实测显存约 9.7 GB；上下文不要超过 8192（模型实际训练的长度，更长时回答会变差），超出后服务端自动滑窗，只保留最近几分钟。12 GB 显卡可以运行。
- 在 `MiniCPM-o 服务地址` 填写服务端的 WebSocket 地址（如 `ws://127.0.0.1:19060/backend`）。服务端同一时间只服务一个语音会话：频道语音和私语先到先得，其余不应答。
- **工具调用**：模型在双工模式下不会调用工具。服务端对每句话用同一个模型的文本模式做一次工具选择（Qwen3 工具格式）：`reply` 让模型直接回答；`silence` 表示这句话不是对机器人说的，模型保持沉默；`backend_task` 把任务交给配套会话的 Agent，结果由模型用自己的声音原样念出来。句子的切分和转写在 AstrBot 本地完成（Silero VAD + SenseVoice，CPU 运行，首次使用时自动下载约 240 MB 模型）。
- `频道沉默倾向` 调节频道里“不是对机器人说的”判断倾向（默认 4，越大越不容易接话）；私语不受影响。`任务应答语` 是把任务交给后台时先说的一句话。
- **音色克隆**：`参考音频` 填本机音频文件路径，取前 10 秒；建议 5～10 秒清晰人声。

## 代理

在 Codex Agent 执行器设置里填写 `Codex 代理`（如 `socks5://127.0.0.1:7890`、`http://127.0.0.1:7890`），Codex 的全部流量都会走这个代理：模型请求、登录，以及实时语音的信令和控制连接。它不影响 AstrBot 的其他部分。

实时语音的媒体本来走 UDP，代理传不了 UDP。设置了代理时，适配器改用对端在 443 端口提供的 ICE-TCP 通道，通过代理建立一条 TCP 连接来传输语音，不需要开放任何 UDP 端口。

## 部署 Mumble 服务器

可以直接使用官方镜像：

```bash
docker run -d --name mumble-server -p 64738:64738/tcp -p 64738:64738/udp mumblevoip/mumble-server
```

首次启动的日志里会打印 `SuperUser` 的密码。机器人只使用 TCP 连接（语音也经 TCP 隧道传输），无需额外开放端口。
